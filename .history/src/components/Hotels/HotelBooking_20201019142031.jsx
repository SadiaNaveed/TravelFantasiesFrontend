import React from "react";

const HotelBooking = (props) => {
  const { hotel, history } = props;
  return (
    
      <div>
         Booking Hotel
          </div>

  
  );
};

export default HotelBooking;